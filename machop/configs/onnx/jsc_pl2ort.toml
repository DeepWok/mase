# basics
model = "jsc-tiny"
dataset = "jsc"

# max_epochs = 10
batch_size = 256
accelerator = "gpu"
# learning_rate = 1e-3
# project = "jsc-tiny"
# load_name = "../mase_output/toy_toy_tiny/software/training_ckpts/best.ckpt"
# load_type = "pl"


[passes.onnxruntime]
do_test = "both" # tests original PyTorch checkpoint performance if "before", post-conversion performance if "after", both if "both"; no test if "NA"
do_train = false

# TODO: add input type (ONNXRuntime must process different inputs accordingly)