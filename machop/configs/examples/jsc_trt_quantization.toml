# basics
model = "jsc-toy"
dataset = "jsc"

max_epochs = 10
batch_size = 256
learning_rate = 1e-3
accelerator = "gpu"

[passes.tensorrt_quantize]
by = "type"
calibrator = ["percentile", "mse", "entropy"]
num_calibration_batches = 10
percentiles = [99]
report = true

[passes.tensorrt_quantize.default.config]
quantize = true
calibrator = 'histogram'
precision ='FP16' # 'FP16'

[passes.tensorrt_quantize.default.config.input]
calibrator = 'histogram'
quantize_axis = false

[passes.tensorrt_quantize.default.config.weight]
calibrator = 'histogram'
quantize_axis = false

[passes.tensorrt_quantize.linear.config]
calibrator = 'histogram'
quantize = true
precision = 'FP16'

[passes.tensorrt_quantize.linear.input]
calibrator = 'histogram'
quantize_axis = false

[passes.tensorrt_quantize.linear.weight]
calibrator = 'histogram'
quantize_axis = false


[passes.tensorrt_quantize.fine_tune]
epochs = 1

[passes.tensorrt_analysis]
num_batches = 500
num_GPU_warmup_batches = 5
test = true