# A basic toy example that demonstrates weight and activation pruning
model = "toy_convnet"
dataset = "cifar10"

[passes.prune.weight]
method = "level-pruner"
criterion = "random"
sparsity = 0.2

[passes.prune.activation]
strategy = "fixed-global"
target = 0.1
